{
    "name": "Bank Locator",
    "version": "0.1.1",
    "description": "Bank discovery made easy",
    "image": "heroku/python",
    "repository": "https://github.com/shubhambhargav/bank-locator",
    "keywords": ["python", "django" ],
    "addons": [ "heroku-postgresql" ],
    "env": {
        "SECRET_KEY": {
            "description": "The secret key for the Django application.",
            "generator": "secret"
        }
    },
    "environments": {
        "test": {
            "scripts": {
                "test": "python manage.py test"
            }
        }
    }
}
